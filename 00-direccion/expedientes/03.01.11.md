# Expediente de la tarea 03.01.11

**NUEVA 31/07 (D-10): guardia del cajon reservado por cifrado. Script cajon_reservado.py: la contraseña solo la tiene el CEO, no se guarda, se pide en cada operacion y nada se descifra a disco**

Este fichero es la **historia completa** de la tarea: quien rechazo que, en que ronda y por
que. Salio de la celda ESTADO de `00-direccion/WBS.md` el 12/08/2026, **recortada y pegada
sin resumir ni una palabra**, porque esa celda tenia 2.654 caracteres y el WBS se lee entero
en cada arranque para averiguar una sola cosa: que toca ahora.

El **estado** de esta tarea NO vive aqui. Vive en el WBS, que sigue siendo su unica fuente
(regla 28 de CLAUDE.md). Aqui vive su historia, y este fichero es la unica fuente de eso.
Se comporta igual que los registros de solo-anadir: se corrige anadiendo, no reescribiendo.

---

## Celda ESTADO integra, tal y como estaba en el WBS

**en_curso** — script escrito y probado por ejecucion 31/07: 6 de 6 casos prohibidos bloqueados (contraseña por tuberia, abrir sin declarar variante, lectura a pelo del fichero cifrado, contraseña incorrecta, y ninguna orden de terminal vuelca datos en claro). VERIFICADO POR EJECUCION (03/08/2026): `04-resultados/registro-cajon.md` registra una entrada SELLAR del 31/07 a las 18:31:29 UTC con resultado sellado. La funcion `sellar()` en `cajon_reservado.py` aborta con mensaje «El cajon ya esta sellado» si se intenta llamar una segunda vez. El CEO confirmo el 03/08/2026 que la contraseña la tecleo en una terminal real del sistema (nunca mediante el prefijo de ejecucion rapida de la sesion de Claude Code), porque `_pedir_password()` exige `sys.stdin.isatty()` y devuelve BLOQUEADO si no es terminal interactiva (D-10: la contraseña no se guarda en ningun sitio, ni por tuberia, ni por variable de entorno). ARTEFACTO: `04-resultados/registro-cajon.md` escrito por `03-motor/scripts/cajon_reservado.py` y citado en D-10 (localizado en 07.01.01(c), 02/08). CORRECCIÓN DE HECHO (05/08/2026, comprobada por ejecución). Esta celda decía «que `critico-codigo` reejecute las 6 inyecciones de `verificar_barreras.py`». Era falso por dos sitios: `verificar_barreras.py` define **5** pruebas — `t_hooks`, `t_datos_git`, `t_gitignore`, `t_reservado_shell`, `t_mensaje` — y **ninguna toca `cajon_reservado.py`**; y esta misma celda dice «6 de 6 casos» pero enumera 5. Las inyecciones del cajón del 31/07 se hicieron a mano y no quedaron en ningún script: el único rastro es la línea SELLAR del 31/07 en `04-resultados/registro-cajon.md`; las dos líneas COMPROBAR son del 03/08, no del 31/07. Una tarea no puede pedir que se repita algo que no existe. PASADA DEL 05/08 (`critico-codigo`, revisada por `validador`): verificado por ejecución que la contraseña por tubería se bloquea, que `abrir` sin motivo se rechaza ANTES de pedir contraseña y que un segundo `sellar()` aborta antes de pedirla. QUEDA PENDIENTE, y por eso esta tarea sigue abierta: (a) que el CEO teclee en una terminal real del sistema `cd /home/server/projects/bot-trading && .venv/bin/python 03-motor/scripts/cajon_reservado.py comprobar` — ningún agente puede hacerlo porque `_pedir_password()` exige `sys.stdin.isatty()`, y esto no es una limitación a arreglar sino el guardia funcionando (regla 26 de CLAUDE.md); (b) que el patrón `Bash(* 02-datos/reservado*)` de `.claude/settings.json` deje de tener el agujero descrito en 03.01.08, que se encola en 03.01.24. Hasta las dos cosas, el cajón está protegido por permisos de Claude Code y por cifrado, pero no a nivel de sistema operativo.
