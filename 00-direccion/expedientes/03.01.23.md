# Expediente de la tarea 03.01.23

**Encadenador de trabajo desatendido: una conversación por tarea**

Este fichero es la **historia completa** de la tarea: quien rechazo que, en que ronda y por
que. Salio de la celda ESTADO de `00-direccion/WBS.md` el 12/08/2026, **recortada y pegada
sin resumir ni una palabra**, porque esa celda tenia 2.812 caracteres y el WBS se lee entero
en cada arranque para averiguar una sola cosa: que toca ahora.

El **estado** de esta tarea NO vive aqui. Vive en el WBS, que sigue siendo su unica fuente
(regla 28 de CLAUDE.md). Aqui vive su historia, y este fichero es la unica fuente de eso.
Se comporta igual que los registros de solo-anadir: se corrige anadiendo, no reescribiendo.

---

## Celda ESTADO integra, tal y como estaba en el WBS

**REDUCCIÓN ORDENADA POR EL CEO (04/08):** la pieza se había crecido con gobernanza que él nunca pidió. Se borraron: `lista-verde.txt` (y toda su barrera de autorización) · los topes en dólares (`MAX_BUDGET_USD_SESION`, `MAX_BUDGET_USD_DIARIO`, `gasto-*.tsv`) · `crontab.propuesto.txt` (arranques programados) · `hook-notificacion.ejemplo.json` (aviso al móvil) · `cola.txt`, `progreso.tsv` y `--disallowedTools`. Petición literal del CEO: *"en vez de enlazar tantas tareas seguidas se parará antes para no consumir tokens y automáticamente abriera otra sesión de claude con la próxima tarea"*. **MOTIVO COMPROBADO para quitar topes en dólares:** no hay clave de API en esta máquina (verificado 04/08: ni en entorno ni en `.claude/settings.json`). Corre sobre plan de suscripción, así que un tope en dólares no limita nada. Lo que sí limita es `--max-turns`. **ENTREGABLES REALES HOY,** y solo estos tres, en `03-motor/desatendido/`: `controlador.sh`, `config.env`, `LEEME.md`. **CÓMO FUNCIONA:** cada sesión nueva ejecuta `/autonomo` y es el **orquestador** quien elige la tarea. Una tarea = una sesión `claude -p` nueva; nunca `--resume` ni `--continue`. Cada sesión deja `DESENLACE.txt` (`CERRADA`/`ESCALADA`/`BLOQUEADA` + código WBS) y `ESTADO.md` (traspaso corto). `ESCALADA`, `BLOQUEADA`, desenlace inválido, traspaso ausente o error → para. Falla cerrado. **BARRERA DE PARADA:** no se inventó gobernanza nueva. Cuando el orquestador escala al CEO, la cadena para. Es el flujo que ya existe. **REVISIÓN ANTERIOR:** tres rondas sobre la versión completa (ocho fallos) y tres sobre la reducida. Trece mecanismos verificados como MURO por ejecución. Veredicto final de `critico-codigo`: ACEPTAR. **HUECOS DECLARADOS, no maquillados:** (1) **nunca se ha ejecutado de verdad** — todo está probado en simulacro, con `claude -p` sustituido por un doble; una tirada real sin nadie delante sigue sin hacerse y es el criterio de hecho de `03.01.05`. (2) Que una sesión real obedezca el prompt y escriba `DESENLACE.txt` y `ESTADO.md` no está verificado por ejecución. **INCIDENTE del 04/08:** durante las pruebas, un agente lanzó una invocación real de `claude -p` sin querer, por olvidarse de forzar el binario doble. Lo declaró el mismo. Consumo de cuota del plan, no dinero. Se comprobó el transcript herramienta por herramienta: solo `Bash` y `Read`, cero escrituras, sin tocar el repositorio ni `02-datos/reservado/`. Sin daño. Estado — **en_curso**. No se enciende: depende de una tirada real supervisada y de `03.01.08`. **CRITERIO DE HECHO** (no cambia): una tirada que arranque sola sin nadie delante, y la tabla muro/prosa con cada línea "muro" disparada por inyección. (Tabla muro/prosa en `03-motor/desatendido/LEEME.md`, líneas 62-81; 13 mecanismos: 12 MURO verificados por ejecución, 1 NO verificado.)
