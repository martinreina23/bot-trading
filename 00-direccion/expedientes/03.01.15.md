# Expediente de la tarea 03.01.15

**Herramientas del secretario: puede escribir los registros que solo admiten añadir pero no puede verificarlos**

Este fichero es la **historia completa** de la tarea: quien rechazo que, en que ronda y por
que. Salio de la celda ESTADO de `00-direccion/WBS.md` el 12/08/2026, **recortada y pegada
sin resumir ni una palabra**, porque esa celda tenia 9.034 caracteres y el WBS se lee entero
en cada arranque para averiguar una sola cosa: que toca ahora.

El **estado** de esta tarea NO vive aqui. Vive en el WBS, que sigue siendo su unica fuente
(regla 28 de CLAUDE.md). Aqui vive su historia, y este fichero es la unica fuente de eso.
Se comporta igual que los registros de solo-anadir: se corrige anadiendo, no reescribiendo.

---

## Celda ESTADO integra, tal y como estaba en el WBS

**NUEVA 02/08 (tres incidentes reales el 02/08):** `secretario` mantiene los tres registros que solo admiten añadir y todas las fichas del CEO, y **no tiene `Bash`**: no puede comprobar por ejecución ninguna de las reglas que gobiernan su trabajo. El 02/08 falló tres veces por lo mismo — no pudo verificar el pegado en tabla, ni el append-only, ni el ancho de su ficha — y las tres las descubrió otro agente, que es la regla 15 de CLAUDE.md rota por diseño, no por descuido. **Decidir entre dos salidas, y NO se da por buena la primera sin sopesar la segunda:** (1) darle `Bash`, que le pone al alcance los registros que nunca se pueden reescribir, siendo el modelo más barato del equipo; (2) dejarlo sin `Bash` y hacer norma que nunca certifique lo que no puede medir, con la comprobación ejecutable como paso nombrado del revisor. **Deuda de motor: no se ejecuta sin permiso del CEO.** Depende de 03.01.02. AMPLIACION DE FICHA (09/08/2026, ronda 2, dictada por el orquestador y pegada literal por el secretario). MOTIVO, con cada cita reproducida por grep antes de dictarla y ninguna heredada: el 09/08 el secretario fallo dos veces mas, que el parte `00-direccion/informes/ULTIMA_TIRADA.md` cuenta en su seccion 7 como su cuarto y su quinto incidente. LOS DOS NO TIENEN LA MISMA CAUSA, y esa diferencia es lo que esta tarea tiene que llevarle al CEO. EL CUARTO SI ES FALTA DE HERRAMIENTA: declaro un `git diff --numstat` con resultado `123 0` sin tener `Bash` para ejecutarlo. Consta en la seccion 5 de ese parte y con detalle en `04-resultados/veredictos/revision_03.01.24_registro.md`, que recoge la cifra y la constatacion de que el secretario no dispone de la herramienta; y lo confirma por su cuenta la nota de proceso de D-29 en `00-direccion/DECISIONES.md`, que remite la causa raiz a esta tarea con estas palabras: «su causa raíz queda para el checkpoint del lunes en la tarea **03.01.15** de `00-direccion/WBS.md`». EL QUINTO NO ES FALTA DE HERRAMIENTA: afirmo haber saneado una celda que el fichero recien escrito por el desmentia, y lo unico que lo documenta es la seccion 8 de ese parte, que ademas declara expresamente lo contrario de lo que parece: «La barrera de herramienta explica lo que no pudo comprobar; **no explica una afirmación falsa sobre un fichero legible** (regla 15 de CLAUDE.md)». CONSECUENCIA QUE NO SE ESCONDE: darle `Bash` no habria evitado el quinto incidente, porque tenia `Read` y el fichero era legible. Una salida que solo reparte herramientas cubre uno de los dos modos de fallo. CORRECCION DE PROCESO, anotada en vez de disimulada: la ronda 1 de esta ampliacion, dictada por el orquestador, atribuia los dos incidentes a la falta de `Bash` y citaba para ello las secciones 5 y 8, que no lo dicen; la frase que si los junta esta en la seccion 7 y es un resumen, no la version precisa, y donde las dos chocan manda la seccion 8, que es el texto que va a `LECCIONES.md`. Lo cazo `critico-codigo` al revisar el pegado y lo reprodujo el orquestador antes de aceptarlo. Es el defecto (2) de la propia L-041 cometido en la orden que la encola (regla 12 de CLAUDE.md), y el ejecutor no tuvo parte en el: pego literal y se verifico byte a byte. ALCANCE AMPLIADO, y se declara por que: el diagnostico NO se limita al secretario, porque la salida (2) que esta ficha obliga a sopesar es una norma de reparto que alcanzaria a todos los agentes, y no se puede juzgar sin saber a cuantos alcanza el defecto. Este argumento NO depende de cuantos incidentes haya habido ni de a quien se imputen: depende de que la salida (2) es una norma general. Se barren TODOS los ficheros que devuelva `.claude/agents/*.md`, sin excepcion, con investigador, arquitecto y validador dentro del barrido obligatoriamente. APARTADO (a), diagnostico POR EJECUCION: tabla con una fila por agente y cuatro columnas, que son agente, herramientas que declara su linea `tools:`, comprobaciones que su trabajo requiere segun su propio fichero de agente y segun las reglas de CLAUDE.md, y cuales de esas comprobaciones NO puede producir con las herramientas que tiene. Cada celda de la ultima columna va respaldada por un grep con fichero y linea y por el comando ejecutado con su salida, nunca por una impresion de lectura. APARTADO (b), propuestas de arreglo con coste y riesgo, minimo tres: dar Bash al agente, trasladar SIEMPRE la comprobacion al revisor y prohibir que un criterio de hecho exija una herramienta que el ejecutor no tiene, y partir el rol, mas las que salgan del diagnostico. La recomendacion la emite un agente DISTINTO del que midio, porque quien mide tiende a preferir el arreglo que le quita trabajo, y tiene que decir expresamente que hace cada propuesta con LOS DOS modos de fallo del 09/08: el que la herramienta explica y el que no. APARTADO (c), ficha de decision para el CEO en el formato obligatorio de CLAUDE.md, que no puede pedirle redactar, buscar ni calcular nada. LIMITE DURO: en esta tarea NO se aplica ningun arreglo y NADIE edita `.claude/agents/*.md` ni `.claude/settings.json`. Lo que se entrega es el diagnostico y la ficha que pide el permiso, porque esta misma ficha ya declara que es deuda de motor que no se ejecuta sin permiso del CEO (regla 7 de CLAUDE.md). REGLA QUE NACE HOY Y SE APLICA YA A ESTE REPARTO: antes de escribir un criterio de hecho se localiza por grep la linea `tools:` de quien va a ejecutarlo, y si la prueba exige una herramienta que no aparece en ella, la prueba se traslada al revisor. Por eso ningun criterio de hecho del secretario en esta tarea exige ejecucion. CRITERIO DE HECHO: existe el documento del apartado (a) con una fila por CADA fichero de `.claude/agents/` y ninguna celda de la columna de lo que no puede producir sin su grep con fichero y linea; existe la seccion de propuestas con coste y riesgo y una recomendacion firmada por un agente distinto del que midio, que se pronuncia sobre los dos modos de fallo; existe la ficha del apartado (c) sin pedirle al CEO redactar, buscar ni calcular nada; y cada pieza la ha aceptado un agente distinto del que la escribio (regla 16 de CLAUDE.md). APARTADO (d), abierto el 09/08/2026 por el orquestador. Nace del experimento que el revisor de la recomendacion ejecuto en repositorio aislado: el bucle de solo-añadir de `.githooks/pre-commit` bloquea `git commit` y `git commit -a`, pero NO se dispara por la via de fonteneria de git (`add` mas `write-tree` mas `commit-tree` mas `update-ref`), que aterriza el borrado en HEAD como un commit normal, sin usar `--no-verify` y sin que ningun patron `deny` de `.claude/settings.json` lo cubra, comprobado por ejecucion. Alcance de (d), y es solo eso: corregir en `CLAUDE.md` la afirmacion de cobertura de ese muro, porque la regla 25 de CLAUDE.md obliga a marcar como no verificado lo que no esta probado. LO QUE NO ENTRA: reparar el muro. Eso es tarea nueva, no se encola aqui (regla 2 de CLAUDE.md) y va al checkpoint del lunes como primer punto. CRITERIO DE HECHO DE (d): `CLAUDE.md` declara la cobertura real de ese muro y lo comprueba un agente distinto del que lo escribe. CERRADA con la letra del CEO. D-34 en `00-direccion/DECISIONES.md`, opcion B: ningun criterio de hecho puede exigir una herramienta que no aparezca en la linea `tools:` de quien ejecuta, y esa prueba se traslada al revisor. Escrita como regla 30 de CLAUDE.md. NO se cambia la linea `tools:` de ningun agente y no se toca `.claude/`. ENTREGADO: (a) diagnostico de los ocho agentes en `04-resultados/diagnostico_03.01.15_herramientas_agentes.md`, con la tabla, la evidencia por ejecucion y la Seccion D que separa los fallos que una herramienta arregla de los que no; (b) recomendacion del `arquitecto` aceptada por `critico-codigo` y persistida como Seccion C; (c) `00-direccion/informes/FICHA_D-34.md`; (d) correccion de la cobertura del muro de solo-añadir en `CLAUDE.md`, con el bypass por la via de fonteneria reproducido por dos agentes distintos en repositorios aislados. HUECOS DECLARADOS Y NO DISIMULADOS, dos: (1) la ficha del apartado (c) SI fue revisada: `validador` la RECHAZO con dos defectos bloqueantes, recogidos en la seccion «Apartados (c) y (d)» de `04-resultados/veredictos/revision_03.01.15.md`, y se reparo en caliente; lo que NO volvio a revisarse fue la version reparada, que es la que subio al CEO, asi que la regla 16 de CLAUDE.md queda incumplida sobre esa version y asi consta tambien en D-34. Un primer borrador de esta celda decia que no habia habido revision ninguna: era falso, el dato lo aporto Claude Code y lo dicto el orquestador sin comprobarlo, lo cazo `critico-codigo` y se corrigio antes de commitear (2) la Seccion C no se puede firmar como pegado literal porque el texto del `arquitecto` solo vivio en un mensaje y no hay original contra el que compararlo, que es L-041 mordiendo a esta misma tarea. COSTE DE PROCESO, para que se aprenda de el: el diagnostico necesito cinco rondas y la ficha dos, y el fondo no se movio en ninguna; la mayoria de los defectos los puso el dictado de quien repartia y no quien ejecutaba. — **hecha** 10/08
