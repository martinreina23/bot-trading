# Expediente de la tarea 03.01.03

**Ejecución desatendida: arranques programados, permisos AMPLIOS por defecto (git como red de seguridad) y topes de gasto**

Este fichero es la **historia completa** de la tarea: quien rechazo que, en que ronda y por
que. Salio de la celda ESTADO de `00-direccion/WBS.md` el 12/08/2026, **recortada y pegada
sin resumir ni una palabra**, porque esa celda tenia 2.724 caracteres y el WBS se lee entero
en cada arranque para averiguar una sola cosa: que toca ahora.

El **estado** de esta tarea NO vive aqui. Vive en el WBS, que sigue siendo su unica fuente
(regla 28 de CLAUDE.md). Aqui vive su historia, y este fichero es la unica fuente de eso.
Se comporta igual que los registros de solo-anadir: se corrige anadiendo, no reescribiendo.

---

## Celda ESTADO integra, tal y como estaba en el WBS

**FICHA (03/08, D-20):** **Cómo se arranca:** `claude -p` en modo no interactivo lanzado desde el `cron` del sistema. Los arranques programados son dos líneas de crontab y no requieren ninguna pieza externa. **Cómo se avisa al humano sin instalar nada, idea 5 de las auditorías:** hook nativo de tipo `http`, que hace el POST él mismo sin script intermedio, con los eventos `StopFailure` (admite matcher por tipo de error), `Notification` con matchers `idle_prompt` y `permission_prompt` —que detectan la tirada parada esperando una aprobación que nadie va a dar en un cron desatendido— y `SessionEnd`. El cuerpo del POST **no es configurable**, así que un destino que acepte texto plano sale a cero líneas de código y uno que exija formato propio a tres o seis líneas de hook `command` con `curl`. **HUECO QUE ESTA TAREA NO PUEDE CERRAR (verificación del 03/08, CORREGIDA el 04/08 — ver el paréntesis siguiente):** no existe ningún tope de gasto configurable por el proyecto. Solo existe el límite del propio plan, que deja de cortar en cuanto se activan créditos de uso. (CORRECCIÓN DE HECHO (04/08, reproducida por el `orquestador` y por `constructor-motor` con `claude --help` sobre la versión 2.1.221 instalada, regla 9 de CLAUDE.md nivel 1): SÍ existe un tope de gasto configurable — `--max-budget-usd <amount>`, "Maximum dollar amount to spend on API calls", que solo funciona con `--print`, que es exactamente el modo que esta ficha prescribe para el cron. El hueco que D-20 dio por cerrado queda REABIERTO. AVISO OBLIGATORIO: el help dice "API calls"; si el proyecto corre sobre plan de suscripción, el tope puede ser inerte. NO se declara activo hasta que se pruebe por inyección con un tope deliberadamente ínfimo y se compruebe que corta de verdad (regla 25 de CLAUDE.md; un tope presente en el help no es un tope probado, L-009).) Los topes que SÍ se pueden poner son de TRABAJO, no de dinero: turnos máximos por agente y nivel de esfuerzo de razonamiento. CONFIRMADO CON MATIZ (04/08, `constructor-motor` sobre el binario 2.1.221): `--max-turns` SÍ existe y funciona solo con `--print`, pero está OCULTO — no aparece en `claude --help`, se localizó inspeccionando el binario. Hay que escribirlo sabiendo que existe. `--effort` existe en el subcomando `claude agents`. **Esta tarea entrega los topes de trabajo y declara el hueco de dinero; no promete un tope de gasto.** Cualquier salida real implica gasto nuevo o cambio de forma de pago, y por tanto ficha para el CEO (regla 23 de CLAUDE.md). **Criterio de hecho:** una tirada arranca sola por cron sin nadie delante, un aviso llega fuera de la sesión cuando se dispara `StopFailure`, y lo comprueba un agente distinto del que lo construye. — **pendiente**
