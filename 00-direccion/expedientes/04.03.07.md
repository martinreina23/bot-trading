# Expediente de la tarea 04.03.07

**NUEVA 03/08 (decisión del CEO, opción B): construir el motor de backtest desde cero contra la especificación de 04.03.06, tras retirar el anterior**

Este fichero es la **historia completa** de la tarea: quien rechazo que, en que ronda y por
que. Salio de la celda ESTADO de `00-direccion/WBS.md` el 12/08/2026, **recortada y pegada
sin resumir ni una palabra**, porque esa celda tenia 6.885 caracteres y el WBS se lee entero
en cada arranque para averiguar una sola cosa: que toca ahora.

El **estado** de esta tarea NO vive aqui. Vive en el WBS, que sigue siendo su unica fuente
(regla 28 de CLAUDE.md). Aqui vive su historia, y este fichero es la unica fuente de eso.
Se comporta igual que los registros de solo-anadir: se corrige anadiendo, no reescribiendo.

---

## Celda ESTADO integra, tal y como estaba en el WBS

FICHA (03/08, dictada por el `orquestador`): **PASO 0, que se ejecuta antes que nada:** retirar del árbol de trabajo el motor anterior con `git revert`, sin reescribir historia, para que quien construya no lo tenga delante. El código sigue existiendo en el commit `0c35959` para siempre, y con él sus 44 funciones de prueba —13 en `test_costs.py`, 20 en `test_execution.py` y 11 en `test_sizing.py`, contadas sobre el commit— que la regla 20 de CLAUDE.md obliga a conservar aunque no se usen. **Verificado antes de ordenarlo:** ningún fichero fuera de esa carpeta la importa, así que la retirada no rompe nada. **Riesgo declarado sin adornos:** quien construye es el mismo agente que escribió el motor de ayer teniendo gb2 delante. **Guardia mecánico, no de confianza:** tiene prohibido ejecutar `git show 0c35959` y abrir `/home/server/projects/gold-bot-2`, y el revisor aplica al código nuevo el mismo procedimiento de comparación que entregue el lote (d) de la tarea 07.01.03, contra las dos fuentes. **Criterio de hecho:** todos los requisitos de `03-motor/ESPECIFICACION_MOTOR_BACKTEST.md` pasan su prueba de aceptación ejecutada aquí; el procedimiento de comparación da INDEPENDIENTE contra gb2 y contra `0c35959`; y lo acepta un agente distinto del que construye. **Éxito es código fiel a la especificación, no una estrategia rentable** (regla 17 de CLAUDE.md). **CORRECCIÓN DECLARADA (03/08, orquestador):** esta ficha justificaba conservar el commit `0c35959` citando la regla 20 de CLAUDE.md. **La cita es una analogía, no una cita literal:** el texto de esa regla es «Se guardan TODAS las pruebas, también las fallidas. Saber cuántas cosas se probaron es lo que permite juzgar si la ganadora es real o casualidad», y habla de variantes de hipótesis, no de suites de pruebas unitarias. Se retira la cita y se deja el motivo verdadero, que es más fuerte: **ese commit es la prueba del incidente del 03/08 y sin él no se puede auditar lo que pasó.** Si el commit de retirada ya se hizo con la cita en su mensaje, **NO se reescribe el historial**: queda corregido aquí. **NOTA DE UNIDAD (03/08):** el orquestador retiró por falsa la cifra «44» **referida al contenido de `.pytest_cache/v/cache/nodeids`, que tiene 39 entradas**. Como número de funciones de prueba del commit, 44 es correcto y se conserva. Se declara la unidad en vez de retirar un dato bueno por contagio del error contiguo. — **hecha** 04/08 — `03-motor/backtester/`. Motor de backtest construido desde cero contra la especificación de 04.03.06. **PROCEDENCIA, que era la pregunta que originó esta tarea: INDEPENDIENTE de las dos fuentes.** Medido con el procedimiento pre-registrado del lote (d) de 07.01.03, bajo la segunda lectura de gb2 que autorizó el CEO en D-25: **0% de intersección de símbolos** contra gb2 y contra el commit retirado `0c35959`, con coincidencias de línea de 5,2-12,9% que son boilerplate (`import pandas as pd`, `@dataclass`, `else:`) y **ninguna constante de calibración heredada**. Contraste: `0c35959` disparaba 66,7% y 100% en ese mismo criterio. Reproducido de forma independiente por el `orquestador`. **ADAPTACIÓN DEL PROCEDIMIENTO, declarada y no escondida:** el motor nuevo no tiene ficheros homónimos, así que el revisor comparó cada fichero contra la unión concatenada de cada fuente, **sin tocar el criterio**. **LOS 16 REQUISITOS pasan, ejecutados por el revisor como caja negra con datos y aserciones propios**, sin reutilizar los tests del constructor. Suite: **48 pruebas**. **Los tres casos a lápiz —2.014,55 · 1.878,88 · 1.979,88 USD— los han reproducido ya TRES agentes distintos, cada uno con aritmética propia y calculando antes de mirar el motor.** **R-06(c) verificado por inyección deliberada por dos agentes distintos** (regla 25 de CLAUDE.md): el revisor inyectó su propio camino duplicado, el test cayó con `assert 3 == 0`, revirtió y comprobó identidad byte a byte. Cifras propias del revisor: velas de 4h idénticas bit a bit a `remuestrear()`; 708.781 filas de 1m producen 3.198 velas de 4h, todas en horas UTC {0,4,8,12,16,20}; asimetría de swap 10,375x frente al 8x exigido; dos ejecuciones dan el mismo SHA-256. **LÍMITE DEL CONSTRUCTOR, comprobado y no aceptado por su palabra:** ejecutó `git show eb7ac2f --stat`, que devuelve solo nombres y deltas, cero código; y conocer esos nombres no reproduce la estructura, porque la partición es distinta y el solapamiento es 0%. **HUECO MENOR DECLARADO:** R-14 admite dos lecturas razonables de «timestamp máximo visible»; con la lectura de C-4, que es la autoritativa, es estrictamente creciente. No hay fuga real —la estrategia solo recibe `velas.iloc[:i+1]`— pero la ambigüedad de redacción se corrige en la especificación. **PASO 0-bis (03/08), escrito ANTES de trabajarlo tras el rechazo de `critico-codigo`, que señaló que este trabajo no estaba en el WBS (reglas 1, 2 y 5 de CLAUDE.md):** el `git revert` del PASO 0 retiró los nueve ficheros rastreados, pero **no pudo tocar el bytecode**: el patrón `__pycache__/` de `.gitignore` mantiene esos ficheros fuera del alcance de git, y `.pytest_cache/` queda fuera por su propio patrón, no por ése; son dos patrones distintos, verificado con `git check-ignore -v` sobre las tres rutas, así que git nunca los vio. **Motivo por el que hay que retirarlos:** un `.pyc` conserva nombres de símbolo, constantes, cadenas y docstrings, y se lee con `dis` o `marshal` de la biblioteca estándar; el de `costs.py` conserva literalmente el docstring que atribuye el fichero al trasplante desde gb2. **Qué se retira:** `03-motor/backtester/__pycache__/`, `03-motor/backtester/tests/__pycache__/` y `03-motor/backtester/.pytest_cache/`. **Por qué se borra y no se mueve fuera del proyecto:** escribir fuera del proyecto es uno de los cuatro casos que la tarea 03.01.08 declara prohibidos y debe inyectar; y el `.pyc` es un derivado con pérdida de algo que se conserva entero y para siempre en el commit `0c35959`, así que no se destruye ninguna prueba. **Criterio de hecho:** `find 03-motor/backtester/ -type f` devuelve solo `.gitkeep`; `git show 0c35959 --stat` sigue devolviendo nueve ficheros y 1.825 líneas; `git status --porcelain` no cambia; y el inventario con sus `sha256` queda escrito ANTES del borrado en `04-resultados/inventario_bytecode_04.03.07.md`, con la unidad de recuento declarada y el comando exacto pegado al lado (L-018 de LECCIONES.md). **Esta subtarea no es un muro:** la retirada es higiene. El muro es la comparación forense posterior. **NOTA DE FECHA (04/08):** este paso se escribió el 03/08 y el encabezado decía 04/08. Error del `orquestador`, que dictó la fecha sin medirla, la misma noche en que ya había afirmado dos horas falsas. Detectado por `constructor-datos` con una prueba que no depende del reloj: `stat` sobre `00-direccion/WBS.md` da 2026-08-03 23:43, así que el texto se dejó de editar **antes** de que llegara el 04/08.
