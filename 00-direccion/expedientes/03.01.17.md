# Expediente de la tarea 03.01.17

**Reparar la prueba 7 de verificar_excel.py (entregables de las tareas hechas)**

Este fichero es la **historia completa** de la tarea: quien rechazo que, en que ronda y por
que. Salio de la celda ESTADO de `00-direccion/WBS.md` el 12/08/2026, **recortada y pegada
sin resumir ni una palabra**, porque esa celda tenia 1.526 caracteres y el WBS se lee entero
en cada arranque para averiguar una sola cosa: que toca ahora.

El **estado** de esta tarea NO vive aqui. Vive en el WBS, que sigue siendo su unica fuente
(regla 28 de CLAUDE.md). Aqui vive su historia, y este fichero es la unica fuente de eso.
Se comporta igual que los registros de solo-anadir: se corrige anadiendo, no reescribiendo.

---

## Celda ESTADO integra, tal y como estaba en el WBS

**FICHA (03/08):** **Problema reproducido hoy:** la prueba 7 localiza entregables citados en las celdas de tarea, pero corta cada celda por el primer `**hecha**` y solo examina lo que va después. En `03.01.13` ese marcador es el último carácter de la celda, así que examina **cero ficheros** y no caza que esa tarea, marcada hecha, cita `00-direccion/PENDIENTE-03.01.13-lista-de-reglas.md`, que **no existe en disco ni ha existido nunca en git**. Dos defectos más: busca por nombre de fichero en CUALQUIER parte del repositorio (una ruta falsa en otra rama pasaría), y emite AVISO en lugar de FALLO. **Alcance:** (a) Examinar la celda entera (no partir por `**hecha**`). (b) Exigir la ruta completa citada (no el nombre suelto); una ruta falsa debe ser FALLO. (c) FALLO en lugar de AVISO. (d) Añadir a `05-vista-ceo/prueba_inyeccion.sh` el caso: una tarea cerrada que cite un fichero inexistente debe fallar la prueba 7. (e) **En la ficha de esta tarea, ordena que la celda de `03.01.13` sea corregida** (quitar el texto que dice «ninguna referencia al fichero PENDIENTE queda escrita» que sigue siendo falso después de D-16) **y que quede escrito que se corrigió, no que se cumplía** — no reabrás 03.01.13 (su fondo de lista única está hecho), solo repara la cita falsa. **Criterio de hecho:** la prueba examina celdas completas; `03-motor/scripts/cajon_reservado.py` (que existe) pasa; el fichero PENDIENTE (que no existe) falla; la inyección correspondiente en `prueba_inyeccion.sh` demuestra ambos casos. — **pendiente**

---

## 12/08/2026 — Bloque recortado por el CEO: comprobación cero + clasificación de los 9 avisos de la prueba 7

**Alcance ejecutado hoy, y solo esto:** (1) inserción literal del bloque "Comprobación cero" en
`.claude/agents/critico-codigo.md` y `.claude/agents/validador.md`; (2) clasificación de los 9
avisos de la prueba 7 de `verificar_excel.py`. **NO ejecutado hoy** (prohibido por el CEO en esta
orden): la reparación de la prueba 7 en sí (puntos (a)-(d) de la ficha de arriba), que sigue
`pendiente`.

### Parte A — comprobación cero

Bloque insertado, carácter a carácter, inmediatamente después de la línea `**Modelo:**` y de la
línea en blanco que la sigue, en los dos ficheros. Verificado por ejecución:

```
$ git diff -- .claude/agents/ | grep -E "^-" | grep -v "^---"
(sin salida: cero líneas borradas)
$ grep -c "Comprobación cero" .claude/agents/critico-codigo.md .claude/agents/validador.md
.claude/agents/critico-codigo.md:1
.claude/agents/validador.md:1
```

### Parte B — lista canónica de avisos, ejecutada hoy

```
$ .venv/bin/python 05-vista-ceo/verificar_excel.py
...
7. Las tareas hechas tienen su entregable en disco
  AVISO  entregables: 01.02.04 dice haber entregado atr_local.py y no esta en el repositorio
  AVISO  entregables: 01.02.04 dice haber entregado .mcp.json y no esta en el repositorio
  AVISO  entregables: 03.01.13 dice haber entregado 00-direccion/PENDIENTE-03.01.13-lista-de-reglas.md y no esta en el repositorio
  AVISO  entregables: 03.01.16 dice haber entregado costs.py y no esta en el repositorio
  AVISO  entregables: 03.01.21 dice haber entregado .meta.json y no esta en el repositorio
  AVISO  entregables: 04.03.07 dice haber entregado costs.py y no esta en el repositorio
  AVISO  entregables: 04.03.07 dice haber entregado test_sizing.py y no esta en el repositorio
  AVISO  entregables: 04.03.07 dice haber entregado test_costs.py y no esta en el repositorio
  AVISO  entregables: 04.03.07 dice haber entregado test_execution.py y no esta en el repositorio
...
RESULTADO: sin fallos. 9 avisos.
```

**9 de 9, coincide con lo que exige la orden.** Se clasifican uno por uno.

Método común a los 9: `grep -n` sobre `00-direccion/WBS.md` (columna ESTADO, no la de alcance) y
`grep -rl` sobre `00-direccion/expedientes/`, para localizar en qué fichero(s) vive la cita; `ls`
de la ruta exacta citada (siempre falla: los 9 son avisos); `find . -iname` sobre todo el
repositorio buscando el nombre suelto (siempre vacío en el árbol de trabajo actual); y el barrido
de historial obligatorio (`git log --all --oneline --diff-filter=A` + `git rev-list --all | while
read c; do git ls-tree -r --name-only $c | grep -qF "<nombre>" && echo "$c TIENE"; done`).

**Causa raíz confirmada por lectura de `05-vista-ceo/verificar_excel.py` (símbolos `expediente_de`
y el bucle de la prueba 7, regla 12 de CLAUDE.md: se referencia por nombre de símbolo, nunca por
número de línea):** `cola_celda` se construye como la celda ESTADO **más el
texto íntegro del expediente enlazado** (`cola_celda += " " + t["expediente"]`), y de ahí se
extrae con una regex CUALQUIER nombre entre comillas invertidas con extensión `.md/.py/.json/
.csv/.txt/.xlsx`, sin distinguir si esa cita es una declaración de entrega o una frase de prosa.
Esto es justo lo que describe la nota del orquestador sobre la casilla 4, y se confirma aquí por
lectura directa del símbolo, no por la nota.

---

#### 1. 01.02.04 — `atr_local.py`

- **Cita literal del aviso:** `01.02.04 dice haber entregado atr_local.py y no esta en el repositorio`
- **Dónde está citado:** `00-direccion/expedientes/01.02.04.md` (1 ocurrencia entre comillas
  invertidas). Frase literal: *"RECORRIDO: entregada, **RECHAZADA** por `critico-codigo` (citaba
  dos veces `atr_local.py`, un fichero que el propio proyecto había BORRADO a propósito en el
  commit `db80a49`; el nombre venía del título planeado de 02.02.01, no del artefacto entregado),
  reparada por el `investigador` —que corrigió además una ruta errónea que el revisor no había
  señalado— y **ACEPTADA en ronda 2**"*. (También aparece `atr_local.py` en la COLUMNA DE ALCANCE,
  no en ESTADO, de la fila `02.02.01` del WBS — esa columna no la lee la prueba 7, así que no
  genera aviso propio; se anota porque explica el origen del nombre.)
- **Comandos y salida:**
  ```
  $ ls -la 03-motor/scripts/atr_local.py
  ls: cannot access '03-motor/scripts/atr_local.py': No such file or directory
  $ find . -iname atr_local.py
  (vacío)
  $ git log --all --oneline --diff-filter=A -- "**/atr_local.py"
  da1e3c7 arranque del proyecto
  $ git rev-list --all | while read c; do git ls-tree -r --name-only $c | grep -qF "atr_local.py" && echo "$c TIENE"; done
  94f73b1d... TIENE
  7cdadea6... TIENE
  ad33c2f0... TIENE
  5ea1b468... TIENE
  36872c52... TIENE
  4d2b7986... TIENE
  da1e3c79... TIENE
  $ git show db80a49 --stat
  commit db80a496... "org: borrar atr_local.py, jubilado por la ficha de 02.02.01"
  03-motor/scripts/atr_local.py | 142 ------------------------------------------
  1 file changed, 142 deletions(-)
  ```
  El fichero SÍ existió (7 commits lo contienen) y fue borrado a propósito el 31/07 en `db80a49`
  ("Regla 28: el documento que se sustituye se borra, git guarda el historial. Lo sustituye
  precios_mercado.py"), conservado para siempre en ese commit.
- **Casilla elegida: 4 — la cita no es una declaración de entrega.** No es "01.02.04 entregó
  atr_local.py": es la narración de un RECHAZO de `critico-codigo` a la primera ronda de 01.02.04
  (un borrador que citaba mal un nombre ya retirado), corregido antes de la ronda aceptada. Encaja
  a la vez en el ejemplo "artefacto de una ronda rechazada" y en "artefacto retirado a propósito y
  conservado en un commit" de la nota del orquestador.
- **Acción realizada:** ninguna. No cambia el estado de 01.02.04 ni se toca el texto citado
  (instrucción explícita de la casilla 4). Queda anotado como material para la futura reparación
  de la prueba 7.

#### 2. 01.02.04 — `.mcp.json`

- **Cita literal del aviso:** `01.02.04 dice haber entregado .mcp.json y no esta en el repositorio`
- **Dónde está citado:** `00-direccion/expedientes/01.02.04.md` (2 ocurrencias). Frase literal 1:
  *"no existe sintaxis de patrón de ruta para herramientas MCP, así que el cajón reservado no se
  podría proteger por ruta: habría que bloquear el servidor entero. Hoy no aplica (no hay
  `.mcp.json`); queda recogido en la regla de admisión de la sección E del informe."* Frase
  literal 2: *"el día que entre un `.mcp.json` habría que bloquear el servidor entero, no la ruta
  del cajón reservado."*
- **Comandos y salida:**
  ```
  $ ls -la .mcp.json
  ls: cannot access '.mcp.json': No such file or directory
  $ find . -iname ".mcp.json"
  (vacío)
  $ git log --all --oneline --diff-filter=A -- "**/.mcp.json"
  (vacío)
  $ git rev-list --all | while read c; do git ls-tree -r --name-only $c | grep -qF ".mcp.json" && echo "$c TIENE"; done
  (vacío)
  ```
  Barrido completo del historial vacío: nunca ha existido.
- **Casilla elegida: 4 — la cita no es una declaración de entrega.** Es literalmente el ejemplo que
  la nota del orquestador cita ("Hoy no aplica (no hay `.mcp.json`)"): una hipótesis a futuro
  ("el día que entre un `.mcp.json`..."), no una entrega de la tarea 01.02.04, cuyo veredicto fue
  CERO servidores MCP.
- **Acción realizada:** ninguna. No cambia el estado de 01.02.04 ni se toca el texto citado.

#### 3. 03.01.13 — `00-direccion/PENDIENTE-03.01.13-lista-de-reglas.md`

- **Cita literal del aviso:** `03.01.13 dice haber entregado 00-direccion/PENDIENTE-03.01.13-lista-de-reglas.md y no esta en el repositorio`
- **Dónde está citado:** `00-direccion/expedientes/03.01.13.md` (la celda ESTADO íntegra ya vive
  ahí desde el 12/08). Frase literal: *"— **PASADA 1** (constructor-motor, 01/08/2026): entregó
  `00-direccion/PENDIENTE-03.01.13-lista-de-reglas.md` porque `WBS.md` y `LECCIONES.md` estaban
  bloqueados por otra sesión; contó "5 citas operativas" en los hooks..."*. También citado en
  `00-direccion/expedientes/03.01.17.md`, en la FICHA de esta misma tarea (arriba en este mismo
  fichero): *"esa tarea, marcada hecha, cita `00-direccion/PENDIENTE-03.01.13-lista-de-reglas.md`,
  que **no existe en disco ni ha existido nunca en git**"*.
- **Comandos y salida:**
  ```
  $ ls -la 00-direccion/PENDIENTE-03.01.13-lista-de-reglas.md
  ls: cannot access '00-direccion/PENDIENTE-03.01.13-lista-de-reglas.md': No such file or directory
  $ find . -iname "PENDIENTE-03.01.13*"
  (vacío)
  $ git log --all --oneline --diff-filter=A -- "**/PENDIENTE-03.01.13-lista-de-reglas.md"
  (vacío)
  $ git rev-list --all | while read c; do git ls-tree -r --name-only $c | grep -qF "PENDIENTE-03.01.13-lista-de-reglas.md" && echo "$c TIENE"; done
  (vacío)
  ```
  Barrido completo del historial vacío: **nunca existió**, confirmando por ejecución lo que la
  propia ficha de 03.01.17 ya afirmaba.
- **Prueba de clasificación:** la cita SÍ es una declaración de entrega ("entregó
  `.../PENDIENTE-...md`") dentro de una tarea (`03.01.13`) hoy marcada **hecha** en el WBS →
  encaja por evidencia en la **casilla 2 (nunca existió + declaración de entrega de tarea
  cerrada)**, cuya acción normal sería devolver `03.01.13` a `pendiente`.
- **COLISIÓN DECLARADA.** Esa acción choca con una orden expresa y anterior de esta misma tarea
  (03.01.17), punto (e) de su propia ficha, arriba en este fichero: *"no reabrás 03.01.13 (su
  fondo de lista única está hecho), solo repara la cita falsa"*. Las dos citas literales
  enfrentadas:
  1. (evidencia que empujaría a reabrir) *"entregó `00-direccion/PENDIENTE-03.01.13-lista-de-
     reglas.md` porque `WBS.md` y `LECCIONES.md` estaban bloqueados por otra sesión"* —
     `00-direccion/expedientes/03.01.13.md`.
  2. (orden que lo prohíbe) *"no reabrás 03.01.13 (su fondo de lista única está hecho), solo
     repara la cita falsa"* — ficha de `03.01.17`, arriba en este mismo fichero.
- **Casilla elegida: ninguna de las 4 — COLISIÓN DECLARADA**, por instrucción expresa de la orden
  de hoy. La decide el orquestador, no el ejecutor.
- **Acción realizada:** ninguna. No se cambia el estado de `03.01.13`, no se toca el texto citado
  en `03.01.13.md` (los expedientes se corrigen añadiendo, y esta corrección concreta —reparar la
  cita falsa sin reabrir— no es la que toca hoy: hoy solo se clasifica y se declara la colisión).

#### 4. 03.01.16 — `costs.py`

- **Cita literal del aviso:** `03.01.16 dice haber entregado costs.py y no esta en el repositorio`
- **Dónde está citado:** `00-direccion/expedientes/03.01.16.md` (1 ocurrencia). Frase literal:
  *"`verificar_excel.py` da 0 FALLOS con el Excel regenerado el 05/08 después de la última edición
  del WBS. Los avisos que quedan no son de esta tarea: el de `costs.py` pertenece a 03.01.17, y el
  caso 3 de `prueba_inyeccion.sh` sigue escapando por el hueco ya declarado más arriba."*
- **Comandos y salida:**
  ```
  $ ls -la 03-motor/backtester/costs.py
  ls: cannot access '03-motor/backtester/costs.py': No such file or directory
  $ find . -iname "costs.py"
  (vacío)
  $ git log --all --oneline --diff-filter=A -- "**/costs.py"
  0c35959 01.02.02: pieza T2 del trasplante -- motor de backtest, aceptado en ronda 2
  $ git rev-list --all | while read c; do git ls-tree -r --name-only $c | grep -qF "costs.py" && echo "$c TIENE"; done
  0c359596... TIENE
  ```
- **Casilla elegida: 4 — la cita no es una declaración de entrega.** Es un meta-comentario que
  remite explícitamente este mismo aviso a la tarea `03.01.17` (esta misma tarea, encargada de
  reparar la prueba 7): la celda de `03.01.16` deja constancia de que el aviso de `costs.py` que
  su propio `verificar_excel.py` seguía emitiendo el 05/08 no es un asunto suyo, sino de la prueba
  7 (el aviso real proviene de `04.03.07`, ver #6 más abajo). No es una entrega de `03.01.16`.
- **Acción realizada:** ninguna. No cambia el estado de 03.01.16 ni se toca el texto citado.

#### 5. 03.01.21 — `.meta.json`

- **Cita literal del aviso:** `03.01.21 dice haber entregado .meta.json y no esta en el repositorio`
- **Dónde está citado:** `00-direccion/expedientes/03.01.21.md` (1 ocurrencia). Frase literal:
  *"Resuelta con dato la duda de los 150 ficheros: cada subagente deja un `.jsonl` y un
  `.meta.json`, 75+75, cero desemparejados comprobado con `comm -3`."*
- **Comandos y salida:**
  ```
  $ ls -la .meta.json
  ls: cannot access '.meta.json': No such file or directory
  $ find . -iname ".meta.json"          # dentro del repo
  (vacío)
  $ find /home/server/.claude/projects/-home-server-projects-bot-trading -iname "*.meta.json" | wc -l
  268
  $ git log --all --oneline --diff-filter=A -- "**/.meta.json"
  (vacío)
  $ git rev-list --all | while read c; do git ls-tree -r --name-only $c | grep -qF ".meta.json" && echo "$c TIENE"; done
  (vacío)
  $ git ls-files | grep -c "meta.json"
  0
  ```
- **Prueba de clasificación:** dentro del repositorio, nunca existió (barrido vacío) — pero la
  frase citada **no dice que la tarea 03.01.21 entregó `.meta.json`**: describe que Claude Code
  (el framework) deja automáticamente un `.meta.json` por cada subagente, en
  `~/.claude/projects/.../subagents/`, **fuera del repositorio git por diseño de la herramienta**,
  citado solo como evidencia de un recuento (75+75). El entregable real de `03.01.21` es
  `04-resultados/consumo-tokens-2026-08-04.md`, que sí existe en el repositorio (`ls -la` lo
  confirma, 16.695 bytes, 05/08). No encaja en casilla 1 (no es una ruta equivocada de un fichero
  del proyecto) ni en casilla 2 (no es una declaración de entrega de la tarea, así que reabrir no
  aplica) ni en casilla 3 en sentido estricto (esa casilla presupone que SE ENTREGÓ algo a
  propósito fuera del repo; aquí nada de `03.01.21` se "entregó" como `.meta.json`, son ficheros
  de infraestructura del propio Claude Code, preexistentes e independientes de la tarea).
- **Casilla elegida: 4 — la cita no es una declaración de entrega**, aplicando el criterio general
  de la casilla (no encaja en ninguna de las tres anteriores) aunque el caso concreto no sea
  ninguno de los cuatro ejemplos listados por el orquestador; se deja constancia explícita de la
  frase que lo demuestra, como exige la orden.
- **Acción realizada:** ninguna. No cambia el estado de 03.01.21 ni se toca el texto citado.

#### 6. 04.03.07 — `costs.py`

- **Cita literal del aviso:** `04.03.07 dice haber entregado costs.py y no esta en el repositorio`
- **Dónde está citado:** `00-direccion/expedientes/04.03.07.md` (1 ocurrencia). Frase literal:
  *"un `.pyc` conserva nombres de símbolo, constantes, cadenas y docstrings, y se lee con `dis` o
  `marshal` de la biblioteca estándar; el de `costs.py` conserva literalmente el docstring que
  atribuye el fichero al trasplante desde gb2."*
- **Comandos y salida:**
  ```
  $ ls -la 03-motor/backtester/costs.py
  ls: cannot access '03-motor/backtester/costs.py': No such file or directory
  $ find . -iname "costs.py"
  (vacío)
  $ git log --all --oneline --diff-filter=A -- "**/costs.py"
  0c35959 01.02.02: pieza T2 del trasplante -- motor de backtest, aceptado en ronda 2
  $ git rev-list --all | while read c; do git ls-tree -r --name-only $c | grep -qF "costs.py" && echo "$c TIENE"; done
  0c359596... TIENE
  $ git show eb7ac2f --stat
  commit eb7ac2f... "04.03.07: retirada del motor de backtest anterior -- decision del CEO, opcion B"
   03-motor/backtester/costs.py                | 184 -----------
   ... (9 ficheros, 1.825 borrados en total)
  $ git show eb7ac2f -s
  "Se retira del arbol, NO del historial: el codigo y sus 44 pruebas siguen en 0c35959
   (regla 20 de CLAUDE.md). Motivo: construir de cero sin tenerlo delante."
  ```
- **Casilla elegida: 4 — artefacto retirado a propósito y conservado en un commit.** El propio
  "PASO 0" de la ficha de `04.03.07` (arriba de la celda hecha) ordena el `git revert` deliberado
  del motor anterior, dejando constancia expresa de que sigue vivo en `0c35959` "para siempre".
  `costs.py` es ese motor retirado, no un entregable prometido y ausente de `04.03.07` (que SÍ
  entregó su propio `costs.py` nuevo en `03-motor/backtester/`, dentro del motor construido desde
  cero — hoy no vive en el árbol de trabajo tras retiradas posteriores del bytecode, pero eso es
  higiene de `.pyc`, no del `.py`; el `.py` fuente actual del motor nuevo se identifica y verifica
  aparte, fuera del alcance de esta clasificación).
- **Acción realizada:** ninguna. No cambia el estado de 04.03.07 ni se toca el texto citado.

#### 7. 04.03.07 — `test_sizing.py`

- **Cita literal del aviso:** `04.03.07 dice haber entregado test_sizing.py y no esta en el repositorio`
- **Dónde está citado:** `00-direccion/expedientes/04.03.07.md` (1 ocurrencia, junto con las
  otras dos de los apartados #8 y #9). Frase literal: *"El código sigue existiendo en el commit
  `0c35959` para siempre, y con él sus 44 funciones de prueba —13 en `test_costs.py`, 20 en
  `test_execution.py` y 11 en `test_sizing.py`, contadas sobre el commit— que la regla 20 de
  CLAUDE.md obliga a conservar aunque no se usen."*
- **Comandos y salida:**
  ```
  $ ls -la 03-motor/backtester/tests/test_sizing.py
  ls: cannot access '03-motor/backtester/tests/test_sizing.py': No such file or directory
  $ find . -iname "test_sizing.py"
  (vacío)
  $ git log --all --oneline --diff-filter=A -- "**/test_sizing.py"
  0c35959 01.02.02: pieza T2 del trasplante -- motor de backtest, aceptado en ronda 2
  $ git rev-list --all | while read c; do git ls-tree -r --name-only $c | grep -qF "test_sizing.py" && echo "$c TIENE"; done
  0c359596... TIENE
  ```
- **Casilla elegida: 4 — artefacto retirado a propósito y conservado en un commit** (mismo caso y
  mismo commit `0c35959`/`eb7ac2f` que el apartado #6; forma parte del motor anterior retirado por
  decisión del CEO, opción B, no de una promesa de entrega incumplida de `04.03.07`).
- **Acción realizada:** ninguna. No cambia el estado de 04.03.07 ni se toca el texto citado.

#### 8. 04.03.07 — `test_costs.py`

- **Cita literal del aviso:** `04.03.07 dice haber entregado test_costs.py y no esta en el repositorio`
- **Dónde está citado:** `00-direccion/expedientes/04.03.07.md` (misma frase que #7, arriba).
- **Comandos y salida:**
  ```
  $ ls -la 03-motor/backtester/tests/test_costs.py
  ls: cannot access '03-motor/backtester/tests/test_costs.py': No such file or directory
  $ find . -iname "test_costs.py"
  (vacío)
  $ git log --all --oneline --diff-filter=A -- "**/test_costs.py"
  0c35959 01.02.02: pieza T2 del trasplante -- motor de backtest, aceptado en ronda 2
  $ git rev-list --all | while read c; do git ls-tree -r --name-only $c | grep -qF "test_costs.py" && echo "$c TIENE"; done
  0c359596... TIENE
  ```
- **Casilla elegida: 4 — artefacto retirado a propósito y conservado en un commit** (mismo caso que
  #6 y #7).
- **Acción realizada:** ninguna. No cambia el estado de 04.03.07 ni se toca el texto citado.

#### 9. 04.03.07 — `test_execution.py`

- **Cita literal del aviso:** `04.03.07 dice haber entregado test_execution.py y no esta en el repositorio`
- **Dónde está citado:** `00-direccion/expedientes/04.03.07.md` (misma frase que #7 y #8, arriba).
- **Comandos y salida:**
  ```
  $ ls -la 03-motor/backtester/tests/test_execution.py
  ls: cannot access '03-motor/backtester/tests/test_execution.py': No such file or directory
  $ find . -iname "test_execution.py"
  (vacío)
  $ git log --all --oneline --diff-filter=A -- "**/test_execution.py"
  0c35959 01.02.02: pieza T2 del trasplante -- motor de backtest, aceptado en ronda 2
  $ git rev-list --all | while read c; do git ls-tree -r --name-only $c | grep -qF "test_execution.py" && echo "$c TIENE"; done
  0c359596... TIENE
  ```
- **Casilla elegida: 4 — artefacto retirado a propósito y conservado en un commit** (mismo caso que
  #6, #7 y #8).
- **Acción realizada:** ninguna. No cambia el estado de 04.03.07 ni se toca el texto citado.

### Resumen de la clasificación

| # | Tarea | Fichero | Casilla | Acción |
|---|---|---|---|---|
| 1 | 01.02.04 | `atr_local.py` | 4 | ninguna |
| 2 | 01.02.04 | `.mcp.json` | 4 | ninguna |
| 3 | 03.01.13 | `00-direccion/PENDIENTE-03.01.13-lista-de-reglas.md` | **COLISIÓN DECLARADA** | ninguna — decide el orquestador |
| 4 | 03.01.16 | `costs.py` | 4 | ninguna |
| 5 | 03.01.21 | `.meta.json` | 4 | ninguna |
| 6 | 04.03.07 | `costs.py` | 4 | ninguna |
| 7 | 04.03.07 | `test_sizing.py` | 4 | ninguna |
| 8 | 04.03.07 | `test_costs.py` | 4 | ninguna |
| 9 | 04.03.07 | `test_execution.py` | 4 | ninguna |

**Cero cambios de estado en el WBS.** Ninguna casilla 1 (nada que corregir de ruta), ninguna
casilla 2 pura (el único candidato colisiona con una orden anterior y se declara aparte), ninguna
casilla 3. **8 de 9 en casilla 4, 1 de 9 en COLISIÓN DECLARADA.** Los 9 quedan intactos en disco:
no se ha tocado ni una palabra de `01.02.04.md`, `03.01.13.md`, `03.01.16.md`, `03.01.21.md` ni
`04.03.07.md`.

### Verificación final tras los cambios de hoy

Los únicos ficheros tocados hoy son `.claude/agents/critico-codigo.md`,
`.claude/agents/validador.md`, este expediente y la celda ESTADO de `03.01.17` en el WBS (ver
abajo). No se editó `00-direccion/WBS.md` en ninguna otra fila, así que el `awk` de columnas y la
regeneración de Excel se ejecutan solo por disciplina, no porque haya un cambio de fondo que
verificar en las 9 clasificaciones:

```
$ awk -F'|' '/^[ \t]*\| *[0-9][0-9]\.[0-9][0-9]\.[0-9][0-9] *\|/ && NF!=7 {print NR": "NF}' 00-direccion/WBS.md
(sin salida: 0 filas fuera de forma)
$ .venv/bin/python 05-vista-ceo/generar_excel.py
(regenerado)
$ .venv/bin/python 05-vista-ceo/verificar_excel.py
RESULTADO: sin fallos. 9 avisos.
```

**Siguen los mismos 9 avisos, y es lo esperado:** hoy no se ha tocado el código de la prueba 7
(prohibido por esta orden) ni se ha reabierto ninguna tarea ni corregido ninguna cita falsa dentro
de un expediente (las únicas dos acciones que habrían hecho desaparecer un aviso eran la 1 y la 2,
y ninguno de los 9 casos cayó ahí). Los 8 de casilla 4 seguirán apareciendo hasta que se repare la
prueba 7 (fuera de alcance hoy); el de COLISIÓN queda para que el orquestador decida.
