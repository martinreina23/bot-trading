# DEUDA DE MOTOR — congelada hasta la puerta GM (01/09/2026)

Estas tareas nacieron de que el motor se inspecciona a sí mismo. Ninguna hace que el
bot exista. Salen del WBS para que dejen de ocupar contexto y de contar como trabajo
pendiente. NO se ejecutan. NO se citan como tareas vivas. Se revisan en GM, no antes.

Congeladas el 12/08/2026 por orden directa del CEO.

**ACTUALIZACION 12/08/2026 — D-36 de `00-direccion/DECISIONES.md`.** `03.01.24` y `03.01.25`
**salen de aqui y vuelven a `00-direccion/WBS.md`**, con su fila integra: el CEO ha dado la
letra que esperaban. **Las demas siguen congeladas y no se tocan.**

Las filas de abajo están **recortadas y pegadas del WBS tal cual**: ni una palabra
resumida, ni una cifra tocada. Conservan sus 7 campos y su formato de tabla.

| Código | Tarea | Responsable | Depende de | Estado |
|---|---|---|---|---|
| 03.01.12 | **NUEVA 01/08 (petición del CEO):** fijar la estrategia de ramas de git. Criterio del CEO, literal: **que dé agilidad, no complejidad** | Constructores | 03.01.01 | FICHA (01/08): **Estado de partida, comprobado por ejecución el 01/08:** `git branch -a` → una sola rama, `main` · `git remote -v` → vacío, no hay remoto · 16 commits · un solo humano. **Alcance:** decidir y dejar escrito (a) cuándo se trabaja en `main` y cuándo no, (b) si se usan worktrees y para qué, (c) qué NO se hace. **Lo que ya se sabe y no hay que volver a investigar:** Claude Code 2.1.220 trae worktrees nativos (`claude --worktree <nombre>`, y `isolation: worktree` en la ficha de un subagente) · los worktrees comparten el directorio `.git`, así que `core.hooksPath = .githooks` **sigue mordiendo dentro de un worktree** y no se pierde ninguna barrera · sin remoto configurado, el ajuste `worktree.baseRef` por defecto (`"fresh"`, que rama desde el remoto) cae a tu `HEAD` local, así que conviene fijarlo a `"head"` de forma explícita para no depender de un comportamiento de reserva. **Hueco concreto ya localizado:** `.claude/worktrees/` NO está en `.gitignore`, y la documentación oficial lo exige en cuanto se cree el primero, o el contenido del worktree aparece como ficheros sin seguimiento en el árbol principal. **Criterio de hecho:** `.gitignore` contiene `.claude/worktrees/` y se comprueba por ejecución que `git status --porcelain` sigue limpio tras crear un worktree de prueba y borrarlo · la norma de ramas está escrita en `CLAUDE.md` en cinco líneas o menos · queda probado por ejecución que el `pre-commit` y el `commit-msg` bloquean igual desde dentro de un worktree (inyectar un fichero de datos y un mensaje sin código WBS). **Lo que NO entra, y se escribe para que nadie lo reabra:** rama por tarea, pull requests (no hay remoto contra el que abrirlos), y cualquier flujo tipo gitflow. Con un solo humano, git ya da marcha atrás sin ceremonia y `/rewind` cubre el deshacer dentro de una sesión. — **pendiente** |
| 03.01.19 | **NUEVA 03/08 (D-20, idea 2 de las auditorías):** registro mecánico de autoría, para que «nadie valida su propio trabajo» deje de ser solo prosa | Constructores | 03.01.02 | **FICHA (03/08):** **Problema:** la regla 16 de CLAUDE.md y C2 no tienen muro. Hoy dependen de que el orquestador reparta bien y de que nadie se equivoque; no hay forma de comprobar a posteriori quién escribió qué. **Nivel 1, que es lo que ESTA tarea entrega:** un hook `PostToolUse` con matcher de escritura anota en `04-resultados/autoria.jsonl` qué fichero se tocó y qué tipo de agente lo tocó. El campo del tipo de agente viaja en la entrada JSON de todo hook que corre dentro de un subagente. Con eso la regla pasa de incomprobable a auditable. Coste estimado por la auditoría: unas 15 líneas. **Nivel 2, que NO entra aquí:** un `PreToolUse` que deniegue escribir un veredicto cuando quien firma es quien consta como autor del artefacto. Se diseña, no se activa: solo se abre como tarea propia si el registro del nivel 1 demuestra que el problema ocurre de verdad (regla 24 de CLAUDE.md, una restricción solo se añade tras un incidente real). **Criterio de hecho:** se corre una tirada con un agente que escribe y otro distinto que revisa, el fichero de autoría queda escrito con los dos, y lo comprueba un agente distinto del que construyó el hook. **Prohibido:** activar el nivel 2 sin su prueba de inyección (regla 25 de CLAUDE.md). Un hook de bloqueo mal escrito corta escrituras legítimas. — **pendiente** |
| 03.01.20 | **NUEVA 04/08 (incidente del 03/08):** muro para que una sesión no siga trabajando sola después de un /compact | Constructores | 03.01.02 | **FICHA (04/08, dictada por el orquestador):** **Incidente que la origina, verificado (regla 24 de CLAUDE.md):** el 03/08/2026, tras un `/compact` a las 21:29, la sesión recibió una orden del CEO a las 21:44 y trabajó sin invocar a un solo subagente hasta las 21:58 — escribió tres decisiones y cuatro lecciones en registros de solo-añadir, hizo cirugía estructural sobre el WBS y tocó dos scripts del Excel del CEO. Nadie lo revisó. El detalle está en la tarea 07.01.03. NO ES EL «Nivel 2» DE 03.01.19 y no hereda su condición: aquel es un guardia anti-auto-revisión que solo se abre si el registro de autoría demuestra que ese problema ocurre; éste es un guardia anti-`/compact`, con su propio incidente ya ocurrido y medido. **Nivel 1 — reinyección:** hook `SessionStart` con matcher `compact` que devuelva al contexto el flujo de las cuatro capas. Se declara lo que es: prosa mejor entregada, no un muro. **Nivel 2 — el muro de verdad:** un `PreToolUse` que deniegue escribir en `00-direccion/*.md` cuando quien escribe es la sesión principal y no un subagente. **TRES COSAS QUE HAY QUE RESOLVER ANTES DE ESCRIBIR UNA SOLA LÍNEA, y ninguna es opcional:** (1) El hecho que lo sostiene no está probado: que el tipo de agente viaje en el JSON de todo hook viene de un informe cuya tarea se cerró declarándose sin revisar. Se comprueba por ejecución (disparar un hook y volcar su JSON) antes de construir nada (regla 9 de CLAUDE.md, nivel 1). (2) Un `PreToolUse` sobre `Write` sería teatro: `.claude/settings.json` permite `Bash(python3 *)`, así que cualquier agente con terminal escribe cualquier fichero sin usar la herramienta `Write`, y el guardia no se enteraría. Es L-009 de LECCIONES.md (guardia cableado al evento equivocado) y está recogido en L-028. (3) Hay dos infraestructuras de hook, no una: los de git (`core.hooksPath` = `.githooks`, con `commit-msg` y `pre-commit` vivos desde el 31/07) y los de Claude Code (`.claude/settings.json`, hoy sin ninguno). D-24 corrige la afirmación de D-20 que decía que no había ninguno. Hay que decidir cuál de las dos usa cada nivel antes de construir. **Criterio de hecho:** se inyecta el caso prohibido (la sesión principal intentando escribir en `00-direccion/`) y se bloquea de verdad (regla 25 de CLAUDE.md); y lo comprueba un agente distinto del que lo construye. Un guardia presente en el código no es un guardia probado. **Riesgo declarado:** un `PreToolUse` mal escrito corta escrituras legítimas, incluidas las de reparación. Se diseña con la salida prevista antes de activarse. **DEUDA DE MOTOR: no se ejecuta sin permiso del CEO** (regla 7 de CLAUDE.md). **NOTA DE ORDEN INVERTIDO, y se escribe en vez de disimularse:** este código fue citado antes de existir. El orquestador lo usó en sus repartos del 03/08 y de ahí pasó a L-028 de `LECCIONES.md` y al apartado (2) de D-24, dos registros que solo admiten añadir. La tarea se crea ahora para que esas dos citas dejen de apuntar a nada. Incumplidas en el momento de citarlo: reglas 2, 5 y 12 de CLAUDE.md. Lo detectó `critico-codigo` al revisar D-24 y D-25. — **pendiente** |

---

## Qué NO se congeló, y por qué

El encargo del CEO listaba ocho tareas. Se han congelado cinco. Las otras tres se quedan
en el WBS, cada una por un motivo comprobado por ejecución:

| Tarea | Por qué se queda |
|---|---|
| `03.01.15` | **No está pendiente: está `hecha`** (commit `89cdf08`). El encargo la daba por pendiente. Solo se mueve lo pendiente. |
| `03.01.18` | Excluida por el propio encargo: es el contador producto/motor, y se ejecuta. |
| `03.01.14` | **`03.01.16` la lleva en su campo «Depende de»** y `03.01.16` se queda en el WBS. Sacarla dejaría una dependencia apuntando a una tarea inexistente, y `05-vista-ceo/verificar_excel.py` marca eso como fallo (`dependencias existen`: *«depende de X, que no existe en el WBS»*). Rompería la vista del CEO. |

Ninguna de las cinco congeladas aparece en el campo «Depende de» de ninguna tarea que
siga viva en el WBS. Comprobado por ejecución antes de moverlas.
